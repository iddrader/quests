const CurrentQuests = () => {
    return (
        <div className="current block">
            {' '}
            <h2 className="block__title">Current quests</h2>
        </div>
    );
};

export default CurrentQuests;
